services:
  - type: web
    name: advertiser-analytics-api
    env: python
    pythonVersion: 3.10
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
